import styled from "styled-components";

export const RouteCardStyled = styled.div`
  background-color: var(--clr-orange-orange);
  border-radius: 25px;
  margin-top: 1rem;
  padding: 1rem;
  max-width: 52rem;
  & a {
    display: block;
  }
`;
